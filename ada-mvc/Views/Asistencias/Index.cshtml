@model IEnumerable<ada_mvc.Asistencias>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Registrar Asistencia</h2>

<style>
    .mybtn {
        border: 1px solid #F06D7E;
        margin-bottom: 1rem;
    }

        .mybtn > a {
            color: #F06D7E;
        }
</style>

<div class="btn mybtn">
    <a>
        @Html.ActionLink("Crear nuevo", "Create")
    </a>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Empleados.Nombre)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Empleados.Nombre)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.IdAsistencia }) |
                @Html.ActionLink("Ver", "Details", new { id = item.IdAsistencia }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.IdAsistencia })

            </td>
        </tr>
    }

</table>
